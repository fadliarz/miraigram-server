"use strict";
// import Joi from "joi";
// export const InstagramUserValidationSchema = Joi.object({
//   username: Joi.string().min(5).max(32).required(),
//   password: Joi.string().min(8).max(32).required(),
//   deviceSetting: Joi.string().min(64).max(512).required(),
//   proxy: Joi.string().min(8).max(255),
// });
