export type DeviceSettings = {
  app_version: string;
  android_version: number;
  android_release: string;
  dpi: string;
  resolution: string;
  manufacturer: string;
  device: string;
  model: string;
  cpu: string;
  version_code: string;
};
